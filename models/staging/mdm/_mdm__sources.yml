version: 2 

sources:
  - name: mdm
    schema: mdm_taxi_refs
    description: Referential data coming from MDM.
    loader: DataFactory
    tags:
      - refs
    tables:
      - name: rate_code
        description: "{{ doc('source__rate_code') }}"
      - name: trip_mode
        description: "{{ doc('source__trip_mode') }}"
      - name: trip_type
        description: "{{ doc('source__trip_type') }}"
      - name: trip_zone
        description: "{{ doc('source__trip_zone') }}"