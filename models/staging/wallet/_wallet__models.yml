version: 2

models:
  - name: stg_wallet__payment_types
    description: "{{ doc('model__stg_wallet__payment_types') }}"
    columns:
      - name: payment_type_id
        description: "{{ doc('column__stg_wallet__payment_type_id') }}"

      - name: payment_description
        description: "{{ doc('column__stg_wallet__payment_description') }}"

