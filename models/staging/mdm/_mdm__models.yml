version: 2

models:
  - name: stg_mdm__rate_codes
    description: "{{ doc('model_stg_mdm__rate_codes') }}"
    columns:
      - name: rate_code_id
        description: "{{ doc('column_stg_mdm__rate_codes__rate_code_id') }}"
        tests:
          - not_null
          - unique

  - name: stg_mdm__trip_modes
    description: "{{ doc('model_stg_mdm__trip_modes') }}"
    columns:
      - name: trip_mode_id
        description: "{{ doc('column_stg_mdm__trip_modes__trip_mode_id') }}"
        tests:
          - not_null
          - unique

  - name: stg_mdm__trip_types
    description: "{{ doc('model_stg_mdm__trip_types') }}"
    columns:
      - name: trip_type_id
        description: "{{ doc('column_stg_mdm__trip_types__trip_type_id') }}"
        tests:
          - not_null
          - unique

  - name: stg_mdm__trip_zones
    description: "{{ doc('model_stg_mdm__trip_zones') }}"
    columns:
      - name: location_id
        description: "{{ doc('column_stg_mdm__trip_zones__location_id') }}"
        tests:
          - not_null
          - unique